extends ../layout

block content
    h1= title
    table
        tr 
            th id
            th Название
        each item in order_items
            tr
                td= item.id
                td= item.label
